import { mdx } from '@mdx-js/react';
import { ZIndexShowcases } from '@divriots/dockit-react/z-index';
import { getCssCustomProps } from '@divriots/dockit-react/style-showcases';
import { Layout } from '~/layout';
export const getCssValue = name => `var(${name})`;
export const props = getCssCustomProps("--sawmill-elevations")
export const values = props.map(([name]) => getCssValue(name));
export default Layout;

## Z-Index values

Need to get this to output the name of each elevation and allow for Click to Copy

<ZIndexShowcases values={values} />


