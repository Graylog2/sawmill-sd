import { mdx } from "@mdx-js/react";
import theme from "~/theme/src/theme.json";
import { ZIndexShowcases } from "@divriots/dockit-react/z-index";
import { Layout } from "~/layout";
export default Layout;

export const keys = Object.keys(theme.default.elevation).map(
  (zIdx) => theme.default.elevation[zIdx]
);

## Z-Index values

Need to get this to output the name of each elevation and allow for Click to Copy

<ZIndexShowcases values={keys} />
