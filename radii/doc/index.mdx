import { mdx } from '@mdx-js/react';
import { RadiiShowcase } from '../src/RadiiShowcase';
import { getCssCustomProps } from '@divriots/dockit-react/style-showcases';
import { Layout } from '~/layout';
export const props = getCssCustomProps("--sawmill-radii")
export const values = props.map(([name]) => name);
export default Layout;

## Z-Index values

Need to get this to output the name of each elevation and allow for Click to Copy

<RadiiShowcase values={values} />
